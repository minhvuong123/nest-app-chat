export enum Routes {
  AUTH = 'auth',
  USERS = 'users',
  CONVERSTIONS = 'conversations',
  PARTICIPANTS = 'participants',
}

export enum Services {
  AUTH = 'AUTH_SERVICE',
  USERS = 'USERS_SERVICE',
  CONVERSTIONS = 'CONVERSATIONS_SERVICE',
  PARTICIPANTS = 'PARTICIPANTS_SERVICE',
}
